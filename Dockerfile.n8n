FROM n8nio/n8n:latest

# Set permissions for Render deployment
USER root
RUN mkdir -p /home/node/.n8n && chown -R node:node /home/node/.n8n
USER node

# We do not override the CMD so n8n starts normally with its default entrypoint
